apply from: '../library.gradle'
dependencies {
    if (renetik_library_release == "true") implementation "$renetik_android_core"
    else implementation project(':renetik-android-core')

    if (renetik_library_release == "true") implementation "$renetik_android_event"
    else implementation project(':renetik-android-event')

    if (renetik_library_release == "true") implementation "$renetik_android_json"
    else implementation project(':renetik-android-json')

    if (renetik_library_release == "true") implementation "$renetik_android_store"
    else implementation project(':renetik-android-store')
}
